import { makeStyles } from '@material-ui/styles'

export const useStyles = makeStyles({
  routeViewerContainer: {
    maxWidth: 900,
    margin: '24px auto'
  },

  backNavButtonText: { marginLeft: 8 }
})
