import React from 'react'

import Implementation from '../../HookDetails/Implementation'
import Resources from '../../HookDetails/Resources'

const UseApiDetails = () => {
  return (
    <section className="mg-hookDetails">
      <h1 className="mg-hookDetails-title">useApi</h1>
      <Implementation render={() => <p>useApi - to be continued</p>} />
      <Resources list={[]} />
    </section>
  )
}

export default UseApiDetails
