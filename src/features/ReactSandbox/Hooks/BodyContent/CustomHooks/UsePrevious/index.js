import React from 'react'

import Implementation from '../../HookDetails/Implementation'
import Resources from '../../HookDetails/Resources'

const UsePreviousDetails = () => {
  return (
    <section className="mg-hookDetails">
      <h1 className="mg-hookDetails-title">usePrevious</h1>
      <Implementation render={() => <p>usePrevious - to be continued</p>} />
      <Resources list={[]} />
    </section>
  )
}

export default UsePreviousDetails
