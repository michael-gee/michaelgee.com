import React from 'react'

import Implementation from '../../HookDetails/Implementation'
import Resources from '../../HookDetails/Resources'

const UseHasChangesDetails = () => {
  return (
    <section className="mg-hookDetails">
      <h1 className="mg-hookDetails-title">useHasChanges</h1>
      <Implementation render={() => <p>useHasChanges - to be continued</p>} />
      <Resources list={[]} />
    </section>
  )
}

export default UseHasChangesDetails
