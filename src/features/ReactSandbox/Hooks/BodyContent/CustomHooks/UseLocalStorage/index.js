import React from 'react'

import Implementation from '../../HookDetails/Implementation'
import Resources from '../../HookDetails/Resources'

const UseLocalStorageDetails = () => {
  return (
    <section className="mg-hookDetails">
      <h1 className="mg-hookDetails-title">useLocalStorage</h1>
      <Implementation render={() => <p>useLocalStorage - to be continued</p>} />
      <Resources list={[]} />
    </section>
  )
}

export default UseLocalStorageDetails
