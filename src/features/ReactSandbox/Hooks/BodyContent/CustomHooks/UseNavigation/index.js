import React from 'react'

import Implementation from '../../HookDetails/Implementation'
import Resources from '../../HookDetails/Resources'

const UseNavigationDetails = () => {
  return (
    <section className="mg-hookDetails">
      <h1 className="mg-hookDetails-title">useNavigation</h1>
      <Implementation render={() => <p>useNavigation - to be continued</p>} />
      <Resources list={[]} />
    </section>
  )
}

export default UseNavigationDetails
