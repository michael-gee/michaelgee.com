import { Container } from '@/components/Container'
import { Button } from '@chakra-ui/react'

# hello?

<Button colorScheme="blue">Chakra UI button test</Button>

export default ({ children }) => <Container>{children}</Container>